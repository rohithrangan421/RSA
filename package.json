{
  "name": "route_sales",
  "version": "1.0.0",
  "description": "Route Sales Frappe App",
  "private": true,
  "scripts": {
    "postinstall": "cd dashboard && npm install",
    "dev": "cd dashboard && npm run dev",
    "build": "cd dashboard && npm run build"
  }
}
